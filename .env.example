# Atlassian MCP — local credentials
# Copy this file to .env and fill in your values. NEVER commit .env.

# --- REQUIRED: Jira / Confluence (same Atlassian account) ---
JIRA_HOST=your-company.atlassian.net
JIRA_EMAIL=you@yourcompany.com
JIRA_TOKEN=your_atlassian_api_token   # https://id.atlassian.com/manage-profile/security/api-tokens

# --- OPTIONAL: Bitbucket (only needed if you use Bitbucket tools) ---
BITBUCKET_WORKSPACE=your-bitbucket-workspace-slug

# --- OPTIONAL: Git workflow defaults ---
BASE_BRANCHES=develop,test
BRANCH_PREFIX=task/

# --- OPTIONAL: HTTP server (defaults work fine, change only if port 3847 is busy) ---
HTTP_PORT=3847
HTTP_BIND=0.0.0.0
